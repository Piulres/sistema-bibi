# Versão 1.1 — Integrada na `dev` (não produção)

A **v1.1.0** está na branch `dev` após merge do PR #72. **Não** sobe para `main`/produção neste ciclo.

**Produção:** linha 1.0 — [`V1_0.md`](V1_0.md) (`v1.0.2` @ `e30b2b0`)

---

## Estado

| Item | Valor |
|------|-------|
| **Versão** | `1.1.0` (`package.json` na `dev`) |
| **Commit** | `0fc9242` |
| **PR** | [#72](https://github.com/Piulres/sistema-bibi/pull/72) — mergeado na `dev` |
| **Produção** | ❌ Aguardar ciclo pós-1.0.2 |

---

## Objetivos da 1.1

1. **Cadastros de mercado** — campos exigidos por operadoras e portais empresariais (ANS, B2B)
2. **CRUDs confiáveis** — validação CPF/CNPJ, RBAC uniforme nas APIs, feedback de erro na UI
3. **Testes** — API CRUD completo + E2E front-end de cadastros

> Agenda prestador, modos demo/operação e identidade plataforma/clínica foram absorvidos pela **linha 1.0** (`v1.0.2`). Não fazem parte da 1.1.

---

## Entregas v1.1.0

### Schema e serviços

| Entidade | Campos novos |
|----------|--------------|
| **Empresa** | nome fantasia, e-mail, telefone, responsável RH, endereço |
| **Beneficiário** | e-mail, sexo, nome da mãe, matrícula, vínculo titular/dependente |
| **Prestador (User)** | telefone, conselho, nº registro, UF, especialidade |

### Correções CRUD

- `requireInternoModule("cadastros")` em todas as APIs de cadastro
- Validação CPF/CNPJ (`src/lib/validation/br-documents.ts`)
- Status empresa alinhado ao CRM — removido `SUSPENSO` inválido
- `CadastrosView`: erro explícito em falha de carga (403/401)
- `updateUser`: valida vínculo PJ/beneficiário ao trocar role

### Testes

- `tests/api/cadastros-crud.test.ts`
- `tests/unit/br-documents.test.ts`
- `e2e/cadastros-crud.spec.ts` (9 cenários)
- Fix E2E walk-in determinístico (`2073aef`)

---

## Pendências 1.1.x (backlog)

| Item | Prioridade |
|------|------------|
| `Procedure.tissCode` na UI | Média |
| `PricingRule` CRUD | Média |
| PJ: movimentação cadastral | Alta |
| Paginação em listagens | Média |
| Inativação lógica (soft delete) | Média |

---

## Fluxo

```
dev (1.1.0 @ 0fc9242) → pre-release → merge main → deploy manual → tag v1.1.0
```

Produção permanece em **v1.0.2** até esse ciclo.

---

## Referências

- [`V1_0.md`](V1_0.md) — linha em produção
- [`RELEASES.md`](RELEASES.md) — pacotes fechados
- [`crud-operations-map.ts`](../src/lib/crud-operations-map.ts)
