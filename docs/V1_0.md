# Versão 1.0.0 — Sistema Bibi (fechada)

Documentação de referência da **primeira versão estável** publicada em produção.

**Produção:** https://sistema-bibi.netlify.app  
**Tag git:** `v1.0.0`  
**Commit de release:** `de88c0e`  
**Deploy Netlify:** `6a393af3` (22/06/2026)

---

## Escopo da 1.0

POC SaaS HealthTech multi-tenant com quatro portais segregados por `role`:

| Portal | Rota | Papel |
|--------|------|-------|
| Prestador | `/prestador` | Agenda e atendimento clínico |
| Interno | `/interno` | Operação, faturamento Pay Per Use, CRM |
| Empresa PJ | `/pj` | Visão corporativa e consumo |
| Beneficiário | `/beneficiario` | Self-service, agenda, prontuário |

### Núcleo de negócio

- Faturamento **Pay Per Use** (procedimentos efetivamente usados)
- Precificação dinâmica por empresa (`PricingRule`)
- PIX mock, assinaturas recorrentes, webhooks B2B
- Cliente 360° (`getPatientOverview`) no interno e beneficiário
- RBAC interno (`ADMIN`, `FATURAMENTO`, `RECEPCAO`, `READONLY`)
- MFA TOTP, telemedicina mock, TISS XML, white-label

### Cadastros (estado na 1.0)

CRUD básico em `/interno/cadastros` para:

- Beneficiário (nome, CPF, nascimento, telefone, empresa)
- Empresa (nome, CNPJ, status limitado)
- Procedimento (código, nome, categoria, preço)
- Usuário (nome, e-mail, senha, role, vínculos)

Mapa completo: **27 entidades** em `src/lib/crud-operations-map.ts`.

### Limitações conhecidas (motivaram a 1.1)

- Cadastros com campos mínimos vs. mercado (ANS / operadoras)
- Status `SUSPENSO` inválido na UI de empresas
- APIs de cadastro sem guard RBAC uniforme
- Agenda prestador só do dia (corrigido na dev pós-1.0)
- PJ 100% read-only

---

## Validação da 1.0

```bash
npm run lint
npm run test        # 88+ testes unit/API
npm run test:e2e    # 44 E2E
npm run pre-release
```

---

## Tag e branches

| Ref | Commit | Notas |
|-----|--------|-------|
| `v1.0.0` (tag) | ver `git show v1.0.0` | Anotada no release |
| `main` @ release | `de88c0e` | Alinhada com produção |
| `dev` | integração contínua | Features pós-1.0 |

Para republicar a tag no commit canônico de produção:

```bash
git checkout main && git pull
git tag -d v1.0.0
git tag -a v1.0.0 de88c0e -m "Release 1.0.0 — produção"
git push origin v1.0.0 --force
```

---

## Histórico

Detalhes de pacotes e deploys: [`RELEASES.md`](RELEASES.md)  
Auditoria PRs: [`HISTORICO_2026-06-21.md`](HISTORICO_2026-06-21.md)
