# Versão 1.0 — Sistema Bibi

Documentação da **linha 1.0** (produção). Próximo deploy: **`v1.0.1`**.

**Produção:** https://sistema-bibi.netlify.app  
**Branch de release:** `main`  
**Branch de integração:** `dev`

---

## Pacotes da linha 1.0

| Tag | Commit | Estado |
|-----|--------|--------|
| `v1.0.0` | `de88c0e` | Em produção |
| **`v1.0.1`** | merge `dev` (sem v1.1) | **Pendente deploy** → PR #73 |

Detalhes: [`RELEASES.md`](RELEASES.md)

---

## Escopo base (v1.0.0)

POC SaaS HealthTech multi-tenant — quatro portais, Pay Per Use, RBAC, MFA, white-label, walk-in, mapas CRUD/fluxo (PRs #64–#66).

---

## Incrementos v1.0.1 (PRs #69–#71)

### Agenda prestador (PR #69)

- API `GET /api/prestador/agenda?view=day|upcoming|past`
- Abas Dia / Próximos / Histórico
- Histórico: `/prestador/paciente/[id]`

### Modos demo e operação (PR #71)

- Dual SQLite (`demo.db` + `operation.db`)
- Seletor em `/interno/seguranca` (ADMIN)
- Docs: [`OPERACAO_DADOS.md`](OPERACAO_DADOS.md)

### Processo (PR #70)

- Fluxo dev-first: PRs → `dev` → `main`

---

## Cadastros na linha 1.0

CRUD básico em `/interno/cadastros`. Melhorias de mercado (v1.1) estão na `dev` — ver [`V1_1.md`](V1_1.md).

---

## Validação e deploy

```bash
npm run lint && npm run test && npm run pre-release
# merge dev → main (sem tag 1.1)
# npx netlify deploy --prod --no-build
# git tag -a v1.0.1 -m "Release 1.0.1"
```
