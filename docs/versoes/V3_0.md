# ServiceOS v3.0 — Mobile app shell (PWA)

Linha **v3.0.x** — experiência de aplicativo no celular (iPhone/Android).

| Campo | Valor |
|-------|-------|
| **Versão** | `3.0.21` |
| **Tag git** | `v3.0.21` |
| **Foco** | PWA standalone (`/instalar`) + caminho Capacitor |
| **Produção** | ver [`RELEASES.md`](RELEASES.md) |

---

## Próximo pacote (tip `main` — aguarda deploy)

### RBAC Fase 5 — write guards generalizados (#354)

- Todos os handlers mutáveis (`POST`/`PATCH`/`PUT`/`DELETE`) em `/api/interno/*` usam `requireInternoModuleWrite` ou `requireInternoAdmin`.
- `READONLY` com módulo `gestao` pode listar lançamentos mas não criar/editar.
- Testes: `tests/security/rbac-gaps.test.ts` · `tests/api/interno-write-guards.test.ts`.
- Doc: [`FLUXOS.md`](../produto/FLUXOS.md) §9 · [`AUDITORIA_FLUXOS.md`](../produto/AUDITORIA_FLUXOS.md) §5.

---

## v3.0.21 — Documentos de saída + fix assistente runtime (27/07/2026)

Guias clínicas refinadas e ruleOverrides conectados ao chat.

### Documentos de saída
- Melhores práticas em guias PDF (receita, exames, encaminhamento).
- Filenames UTF-8 e export autenticado via `download-export`.

### Assistente
- `ruleOverrides` aplicados no runtime do chat.
- Gateway no modo IA sem exigir `ASSISTANT_PROVIDER` explícito.

### Qualidade
- `documentos-saida` · `clinical-discharge` · `assistant-hybrid` · CI #342/#352 · pre-release OK · deploy `6a670ad1` (`bibi-poc-2026-07-27m`).

---

## v3.0.20 — Estoque Fase 4 + Assistente híbrido + Capacitor B + PJ agendar (27/07/2026)

Integridade de estoque, pipeline híbrido do Assistente, scaffold mobile nativo e agendamento RH.

### Estoque
- Reversão idempotente via timeline (`reversed` / `isReversal`).
- `refreshExpiredLots` antes de baixas; FIFO ignora vencidos.
- Reforço de entrada não reabre QUARENTENA/BLOQUEADO/VENCIDO.
- UI manual: SAIDA / AJUSTE / PERDA.

### Assistente
- Pipeline híbrido LLM → regras → tools com allowlist e fallback.

### Portal PJ
- RH agenda em nome do colaborador (`POST /api/pj/appointments`) com anti-IDOR `companyId`.

### Mobile
- Capacitor Fase B: projetos iOS/Android + service worker shell.

### Qualidade
- `stock` · `assistant-hybrid` · `pj-appointments` · `pwa-mobile-shell` · CI #337/#338/#340/#335.

---

## v3.0.19 — Assistente Fase 3 + marca whitelabel (27/07/2026)

CRUD de regras por tenant, preview efetivo e marca circular PWA/API.

### Assistente
- Painel CRUD de `ruleOverrides` (add/remove gatilhos, desativar tool).
- Preview efetivo global → nicho → tenant; limpeza de lista vazia.

### Marca / PWA
- Brand mark circular whitelabel (`BrandMark`, `/api/brand/mark`).
- Ícones PWA/apple regeneráveis.

### Qualidade
- Fix jornada consultório (uniqueSlot / 400).
- `assistant-rule-engine` · `interno-assistant-settings` · `brand-mark` · CI #325/#329/#330 · deploy `6a66f3a7` (`bibi-poc-2026-07-27j`).

---

## v3.0.18 — Estoque Fase 3 + Assistente regras (27/07/2026)

Produtos sem lote/validade e motor de regras do Assistente.

### Estoque
- `requiresLot=false` no cadastro de produto.
- Lote sintético `SEM-LOTE` para movimentações sem rastreio.
- Entrada/devolução/dispensação sem validade quando produto não exige lote.
- UI: checkbox “Exige lote”, badge SEM LOTE, campos condicionais.

### Assistente
- Motor de regras Fase 2 com overrides por nicho/tenant.

### Qualidade
- `stock` · `assistant-rule-engine` · CI #319/#321/#326 · docs:verify · pre-release OK · deploy `6a66e9d6` (`bibi-poc-2026-07-27i`).

---

## v3.0.17 — RBAC clínico 360° + a11y (27/07/2026)

Fecha backdoor de prontuário no portal interno e melhora teclado/foco.

### Auditoria / Cliente 360°
- `canAccessPatientClinicalDetail` — só ADMIN.
- Overview/export sem `medicalRecords` para RECEPCAO/FATURAMENTO/READONLY.
- OpenAPI: capabilities da auditoria.

### A11y / Agenda
- Focus trap, TabBar roving, focus-visible.
- Confirmação automática no agendamento self-service.

### Qualidade
- `patient-clinical-rbac` · `a11y-focus` · CI #292/#317 · docs:verify · pre-release OK · deploy `6a66e5f9` (`bibi-poc-2026-07-27h`).

---

## v3.0.16 — Reagendar + Assistente Fase 0 + Estoque Fase 2 (27/07/2026)

Beneficiário troca horário sem cancelar+criar; Assistente ganha inventário/RBAC; estoque fecha UI operacional.

### Agenda do beneficiário
- `rescheduleBeneficiaryAppointment` — atualiza o mesmo registro, valida slot livre.
- UI Minha agenda → **Reagendar**; timeline `RESCHEDULED`.

### Assistente
- Fase 0: inventário de rotinas, módulo interno, `Tenant.settings`.
- Chat com persistência local e auto-scroll; painel `/interno/assistente`.

### Estoque Fase 2
- `StockView`: editar/inativar produto, status de lote, Reverter movimentação.
- Taxonomia multi-nicho: `SERVICO` + unidades `KIT|SC|M3`.

### Qualidade
- `scheduling-reschedule`, `assistant-*`, `stock`/`stock-constants` · CI #282/#286/#306/#308.

---

## v3.0.15 — Gestão clínica otimizada (27/07/2026)

UX da gestão clínica CEDIG para a recepção: menos blank, KPIs no topo e formulário mais curto.

### Gestão clínica
- Faixa de KPIs (receita / exames / despesas / lucro) sempre visível em `/interno/gestao`.
- Meta de menus carregada uma vez; período com refresh soft (sem blank da tela).
- Extras clínicos (biópsia, polipectomia, clips) em `<details>` recolhido; CTA sticky com total.
- KPIs detalhados (`/kpis`) só na aba Indicadores.

### Qualidade
- `clinic-finance-month-strip` (WHAT+WHY) · E2E `cedig-gestao` com assert `clinic-finance-kpi-strip`.
- CI #290 verde (Unit + E2E).

---

## v3.0.14 — Auditoria RBAC + estoque + cadastros + documentos (27/07/2026)

Timeline/auditoria filtram conteúdo sensível por perfil; estoque clínico com reversão; cadastros por aba; documentos de saída com guias PDF.

### Auditoria
- `audit-access.ts` — classes clínico / financeiro / PII / segurança / operacional.
- Redação em audit, export, revisões, dashboard e Cliente 360°.
- Restore só ADMIN; FATURAMENTO vê financeiro completo com PII mascarado.

### Estoque
- `stock-reverse.ts` — compensação bidirecional com lote (ENTRADA/SAIDA/DISPENSACAO/…).
- Dispensação no atendimento bloqueada em CANCELADO/FALTOU (API + UI).
- `FLUXOS.md` alinhado à API real.

### Cadastros
- `/interno/cadastros` — fetch por aba + `next/dynamic` (sem bloquear UI no Promise.all das 4 listas).
- `resolveCadastrosTab` para deep-links seguros por nicho.

### Documentos de saída
- `ClinicalReferral` + templates de encaminhamento; hub de guias no atendimento.
- PDF A4: receita (2 vias se controle especial), pedido de exames, encaminhamento.
- Portal beneficiário: `/beneficiario/documentos`.

### Qualidade
- Testes: `audit-access`, `audit-rbac-content`, `stock`, `cadastros-resolve-tab`, `documentos-saida` (WHAT+WHY).
- CI unit+E2E verdes nos PRs #291, #293, #296 e #299.

---

## v3.0.13 — Exportações + equipe no atendimento (27/07/2026)

Correção das exportações autenticadas e equipe no atendimento com receita multi-item.

### Exportações
- `download-export.ts` — fetch+blob com parsing de `Content-Disposition` UTF-8.
- `ExportButtons` e `DownloadLink` substituem `<a download>` em rotas autenticadas.
- TISS XML, LGPD JSON, import/export CSV/JSON e download `.ics` corrigidos.

### Portal Prestador
- `AppointmentParticipant` — equipe vinculada ao atendimento com custos PPU.
- `PrescriptionDocument` — receita multi-medicamento com templates.
- Aba **Equipe** no atendimento; papéis configuráveis por nicho/procedimento.

### Qualidade
- Testes: `exports-matrix`, `download-export`, `appointment-team`, `prescription-document`.
- Fix build: `COMMON_MEDICATIONS` isolado do módulo `server-only`.

---

## v3.0.12 — Dashboard KPIs claros (27/07/2026)

Dashboard Executivo deixa de misturar eixos de cobrança e produção clínica.

### UX
- Seção **Cobrança**: A receber · Recebido · A faturar · Atendimentos hoje.
- Seção **Produção clínica · MM/AAAA**: Valor lançado / Resultado do mês (eixo distinto das faturas).
- Remove card duplicado “Receita” e o rótulo ambíguo “Total faturado”.

### Qualidade
- `summarizeInvoiceMoney` + mês clínico em BRT (`timezone.ts` / `clinic-finance`).
- E2E interno/CEDIG alinhados aos novos headings.

---

## v3.0.11 — Hotfix marcar paga / Blob (27/07/2026)

Corrige persistência de **Marcar paga** no modo operação (Netlify Blobs).

### Correção
- Flush do `operation.db` não ocorre mais no meio de `$transaction` — só após COMMIT.
- `src/lib/sqlite-transaction-flush.ts` + ajuste em `src/lib/db.ts`.
- Regressão: unit + API (MANUAL/idempotência) + E2E do modal Confirmar pagamento.

---

## v3.0.10 — Mês operacional + agenda/calendário (27/07/2026)

Pacote de massa demo sempre atual, suite da jornada do consultório e sincronização de agenda.

### Massa e testes
- Seed `[seed-operation-month]` — ~30 dias relativos (fontes diversas, descontos, PEP, estoque, timeline, CEDIG).
- `tests/api/consultorio-journey.test.ts` + `e2e/jornada-consultorio.spec.ts`.
- Consistência: `operation-month-plan` / `operation-month-consistency`.

### Agenda e calendário
- ICS + OAuth Google/Microsoft + feed; push automático da agenda.
- Disponibilidade do prestador (grade/bloqueios) · docs `CALENDAR_INTEGRATION.md` / `PROVIDER_AVAILABILITY.md`.

### Qualidade
- Fix flaky assistente (`HH:MM` no teste de agendamento).

---

## v3.0.9 — Hotfix timezone America/Sao_Paulo (26/07/2026)

Correção de horários em produção (Netlify/UTC): agendas, slots, dashboards e labels passam a usar o fuso operacional `America/Sao_Paulo`.

### Correção
- `src/lib/timezone.ts` — conversão/formatação/limites de dia civil BRT.
- Agenda interno/prestador, slots do beneficiário, assistente, exports e seed alinhados.
- Testes: `tests/unit/timezone.test.ts`.

---

## v3.0.8 — Jornada consultório + reset operacional CEDIG (26/07/2026)

Pacote de documentação operacional e ferramentas de limpeza da massa CEDIG — sem breaking changes de produto.

### Documentação
- [`produto/JORNADA_CONSULTORIO.md`](../produto/JORNADA_CONSULTORIO.md) — narrativa chegada → agenda → atendimento → fechamento com todas as ramificações.
- Auditoria/inventário reconciliados; lacunas pós-v3.0.7 (setup, versão, OpenAPI).

### Operação CEDIG
- Scripts: `reset-cedig-transactional.mjs`, `cedig-ensure-commercial.ts`, `publish-operation-blob.mjs`.
- Playbook em [`clientes/cedig/OPERACAO.md`](../clientes/cedig/OPERACAO.md) · timeline em [`STATUS.md`](../clientes/cedig/STATUS.md).
- Produção: fluxos de teste zerados; equipe + catálogo + preços preservados.

---

## v3.0.7 — UX mobile portais + dashboard + exports (26/07/2026)

Pacote de clareza nos portais autenticados e formatos de relatório — sem breaking changes.

### Navegação mobile
- Drawer dos portais abre pela **direita** (`MobileNavDrawer` right-fixed, alinhado à landing).
- Trigger sem contagem de módulos; categorias com contraste e separador.
- Doc: [`plataforma/DESIGN_SYSTEM.md`](../plataforma/DESIGN_SYSTEM.md) · [`produto/ARQUITETURA_PORTAIS.md`](../produto/ARQUITETURA_PORTAIS.md).

### Dashboard e gestão
- Dashboard executivo com hierarquia de KPIs e menos ruído visual.
- Gestão clínica com layout responsivo no mobile (cards vs tabela desktop).
- Doc: [`produto/FLUXOS.md`](../produto/FLUXOS.md) §4.0.1 (dashboard) · §4.2.1 (gestão mobile) · [`plataforma/ARQUITETURA.md`](../plataforma/ARQUITETURA.md) §15.

### Relatórios
- Exports canônicos CSV/JSON/TXT/PDF nos portais interno/PJ/prestador.
- Doc: [`produto/FLUXOS.md`](../produto/FLUXOS.md) §4.11 · [`plataforma/API_DOCS.md`](../plataforma/API_DOCS.md) §9 · [`plataforma/ARQUITETURA.md`](../plataforma/ARQUITETURA.md) §17.

### Documentação operacional
- Narrativa consultório (PPU + CEDIG): [`produto/JORNADA_CONSULTORIO.md`](../produto/JORNADA_CONSULTORIO.md)
- Reset transacional CEDIG em produção: `scripts/reset-cedig-transactional.mjs` + `cedig-ensure-commercial.ts` + `publish-operation-blob.mjs` — playbook [`clientes/cedig/OPERACAO.md`](../clientes/cedig/OPERACAO.md) · [`plataforma/OPERACAO_DADOS.md`](../plataforma/OPERACAO_DADOS.md) §Reset de fluxos CEDIG

Testes: **598 Vitest · E2E `mobile-nav.spec.ts` + `cedig-gestao.spec.ts` (mobile overflow) · docs:verify · pre-release OK** · smoke produção 200 (`bibi-poc-2026-07-26h`).

---

## v3.0.6 — Home comercial + nav portais + assistente (26/07/2026)

Pacote de captação e UX nos quatro portais — sem breaking changes.

### Home comercial (#230)
- Funil dor → solução → como funciona → segmentos; copy Pay Per Use multi-nicho.
- Nav com **7 âncoras** em `src/lib/landing/navigation.ts` (`HOME_NAV_ANCHORS`).
- Doc: [`comercial/PLANO_HOMEPAGE.md`](../comercial/PLANO_HOMEPAGE.md) · [`produto/FLUXOS.md`](../produto/FLUXOS.md) §0.4.

### Nav portais (#235)
- Redesign mobile/desktop nos 4 portais — `NavTabs` com menu **Mais**, `ScrollableNavRail`, drawer `< lg`.
- Wrapper sticky compartilhado: `src/lib/navigation/portal-nav.ts` (`PORTAL_NAV_STICKY_CLASS`).
- Doc: [`plataforma/DESIGN_SYSTEM.md`](../plataforma/DESIGN_SYSTEM.md) · [`produto/ARQUITETURA_PORTAIS.md`](../produto/ARQUITETURA_PORTAIS.md) §Navegação.

### Assistente (#232)
- Painel fecha ao clicar em ação `link`/`form_draft` e ao mudar de rota (`AssistantPanel` + `AssistantActionCard`).
- Doc: [`produto/ASSISTENTE_SERVERLESS.md`](../produto/ASSISTENTE_SERVERLESS.md) §UX do painel.

Testes: **587 Vitest · E2E `mobile-nav.spec.ts` · docs:verify · pre-release OK** · smoke produção 200 (`bibi-poc-2026-07-26f`).

---

## v3.0.5 — Atendimento clínico + jornada + UX portais (26/07/2026)

Pacote de produto no portal Prestador e limpeza de superfície (landing/portais) — sem breaking changes.

### Prontuário e documentos (#221)
- Protocolos de exames editáveis (`ExamProtocolTemplate`) e aplicação em lote na aba Exames.
- Atestado estruturado alinhado à CFM; receita comum / `CONTROLE_ESPECIAL` com reativar.
- Doc: `docs/produto/DOCUMENTOS_CLINICOS.md`.

### Jornada e abas (#225)
- `invoiceStatus` no appointment + `deriveCareJourneyBilling()` — stepper chega em Faturado/Pago.
- Grid de abas com `min-w-0`, `shortLabel` até `xl`, remediação do `ScrollableNavRail`.

### Landing e headers (#226, #227)
- Nav da home: Solução, Demo, Segmentos, Portais, Contato, FAQ; marca **Sistema Bibi**.
- Portais: Tour sem `?`; sem faixa Powered by; badges ocultos no mobile.

Testes: **587 Vitest · docs:verify · pre-release OK** · smoke produção 200 (`bibi-poc-2026-07-26e`).

---

## v3.0.4 — TISS + config Cursor + docs schema-sync (26/07/2026)

Pacote de qualidade pós-v3.0.3 — sem breaking changes.

### Faturamento TISS (#212)
- `TissBuildError` em `src/lib/tiss-service.ts`: guia sem itens (`NO_ITEMS`) ou
  beneficiário sem documento (`NO_PATIENT_DOCUMENT`) → rota responde **422**.
- `escapeXml` cobre os 5 caracteres reservados XML.
- Testes: `tests/api/tiss-guide.test.ts` (5 casos).

### Config Cursor (#218)
- Router único `alwaysApply` (~40 linhas) + rules escopadas por glob/tarefa.
- `AGENTS.md` enxuto (índice) + nested `AGENTS.md` por domínio.
- Skill modular com references (CRUD, CEDIG, PIX, auth, release).
- `npm run cursor:verify` — guardrail de drift.

### Documentação (#219)
- Seção **Schema-sync do operation.db** em `OPERACAO_DADOS.md`.
- Refs de produção v3.0.4 alinhadas em 28+ docs.

Testes: **572 Vitest · 152 E2E · docs:verify · cursor:verify · pre-release OK**.

---

## v3.0.3 — Flush Blob + limpeza operação CEDIG (26/07/2026)

Empilha o restante do hotfix de gestão (#214) e a higiene da base de operação:

- **Schema-sync → Blob:** após migrar colunas/tabelas no boot, o `operation.db`
  é persistido no Netlify Blobs (evita reprocesso a cada cold start).
- **UX gestão:** erros de listagem/registro em `/interno/gestao` com mensagem útil.
- **Limpeza operation.db:** anamneses duplicadas da consulta Renan Emigdio /
  Dra. Gabriela Lage unificadas; usuários e massa efêmera de golive/smoke
  removidos. Script: `scripts/cleanup-operation-test-data.mjs`.

---

## v3.0.2 — Hotfix: schema-sync do banco de operação (26/07/2026)

**Incidente:** em produção (CEDIG, modo operação) a tela `/interno/gestao`
retornava 500 (`no such column: bridgeStatus`) ao listar/registrar lançamentos.

**Causa raiz:** o `operation.db` do Netlify Blobs sempre vence o artefato de
build e `prisma db push` não roda na Lambda — o banco ficou congelado no schema
anterior à ponte v2.6 (`ClinicExamLaunch` sem `bridgeStatus`/`appointmentId`/…).

**Correção:** `src/lib/operation/schema-sync.ts` aplica migrações aditivas
idempotentes (CREATE TABLE / ADD COLUMN / CREATE INDEX) no boot da Lambda,
comparando o banco ativo com o artefato de build. Validado com o banco real de
produção (500 → 200/201, ponte `SYNCED`).

---

## v3.0.1 — Qualidade multi-nicho + regras da agenda (26/07/2026)

Pacote de manutenção que empilha as correções da auditoria de fluxos (rodada 3)
e a consistência de vocabulário por tenant. Sem breaking changes.

### Vocabulário do tenant (`useLabels()`)
- Views internas e de portais passam a usar o glossário do nicho em vez de
  strings fixas ("Paciente/Beneficiário"): `BillingView`, `PjView`,
  `CadastrosView`, `AppointmentsView`, `ComunicacaoView`, `SubscriptionsView`,
  `PrestadorExtratoView`, `PatientOverviewView`, `PrestadorPatientHistoryView`,
  `AtendimentoView`, `ClinicFinanceView`.
- `buildPatientHistoryBreadcrumbs` e o título da página de estoque
  (`estoqueTabLabel`) seguem o nicho.
- Consumo do beneficiário: badge "Faturado"/"A faturar" (antes "PAGA"/"ABERTA").

### Regras de negócio (agenda / prestador)
- Máquina de estados do agendamento (`src/lib/appointment-status.ts`, FLUXOS
  §10.1): procedimento bloqueado em `CANCELADO`/`FALTOU`; transições inválidas
  e estados terminais rejeitados (409).
- Guards do beneficiário padronizados em `requireBeneficiary()`; rotas internas
  destrutivas (`void`/`reverse`/`retry`/`revert-recent`) exigem escrita.

### Correções de UX e feedback
- `ClinicalCarePanel`, `ClinicFinanceView` e `BillingView` tratam erro/403 com
  mensagem em vez de tela vazia; portal PJ avisa conta sem empresa vinculada.

### DX / infra
- `npm run setup` (setup idempotente de VM nova) + gotchas de teste em
  `docs/plataforma/TESTES.md`.
- CI: `actions/checkout` e `actions/setup-node` → v6 (Node 24); `test.db` com
  caminho rápido por marker (suíte ~112s → ~30s).

Testes: **563 Vitest · 152 Playwright · docs:verify · pre-release OK**.

---

## Escopo v3.0.0

### Fase A — PWA (publicado)

- [x] Web App Manifest (`display: standalone`)
- [x] Ícones Android/iOS (`public/icons/`) — marca circular whitelabel (`BrandMark`, `npm run icons:generate`)
- [x] Metas Apple (`apple-mobile-web-app-*`)
- [x] Página/guia de instalação (`/instalar`)
- [x] Smoke Netlify no `pre-release` (`scripts/smoke-netlify-pwa.mjs`)
- [x] Highlight na home (`#novidades`) e versão no title/footer

### Fase B — Shell nativo (Capacitor) — em andamento

- [x] Scaffold `mobile/` (iOS + Android)
- [x] Splash + ícone nativos (BrandMark via `npm run mobile:resources`)
- [x] Service worker — cache offline básico (`/instalar` + ícones)
- [ ] TestFlight / distribuição interna (requer Mac + Apple Developer)

### Fora de escopo v3.0.0

- Redesign dos portais
- Novo modelo de dados
- Deploy Netlify automático

---

## Como testar (iPhone)

1. Abrir https://sistema-bibi.netlify.app/instalar no **Safari**
2. Compartilhar → **Adicionar à Tela de Início**
3. Abrir o ícone — modo `standalone` (sem barra do Safari)

---

## Referências

- Operações: [`../plataforma/OPERACOES.md`](../plataforma/OPERACOES.md)
- Capacitor (futuro): [`../../mobile/README.md`](../../mobile/README.md)
- Pacote anterior: [`V2_6.md`](V2_6.md)
