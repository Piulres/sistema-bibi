# Versão 1.0 — Sistema Bibi

Documentação da **linha 1.0** (histórico). Último pacote da linha: **`v1.0.2`**.

**Produção atual:** **`v2.0.0`** — ver [`V2_0.md`](V2_0.md) e [`RELEASES.md`](RELEASES.md).

---

## Pacotes da linha 1.0

| Tag | Commit (app) | Estado |
|-----|--------------|--------|
| `v1.0.0` | `685cc21` | Substituído |
| `v1.0.1` | `e4d8a43` | Substituído |
| **`v1.0.2`** | `e30b2b0` | ✅ Substituído por v1.2.0 |

Detalhes operacionais: [`RELEASES.md`](RELEASES.md)

---

## Escopo base (v1.0.0)

POC SaaS HealthTech multi-tenant com quatro portais segregados por `role`:

| Portal | Rota | Papel |
|--------|------|-------|
| Prestador | `/prestador` | Agenda e atendimento clínico |
| Interno | `/interno` | Operação, faturamento Pay Per Use, CRM |
| Empresa PJ | `/pj` | Visão corporativa e consumo |
| Beneficiário | `/beneficiario` | Self-service, agenda, prontuário |

### Núcleo de negócio

- Faturamento **Pay Per Use** (procedimentos efetivamente usados)
- Precificação dinâmica por empresa (`PricingRule`)
- PIX mock, assinaturas recorrentes, webhooks B2B
- Cliente 360° (`getPatientOverview`) no interno e beneficiário
- RBAC interno (`ADMIN`, `FATURAMENTO`, `RECEPCAO`, `READONLY`)
- MFA TOTP, telemedicina mock, TISS XML, white-label
- Walk-in particular, mapas CRUD/fluxo, melhorias visuais (PRs #64–#66)

Mapa completo: **27 entidades** em `src/lib/crud-operations-map.ts`.

---

## Incrementos v1.0.1 (PRs #69–#71)

### Agenda prestador completa (PR #69)

- API `GET /api/prestador/agenda?view=day|upcoming|past&date=`
- Abas **Dia / Próximos / Histórico** em `AgendaView`
- Histórico do paciente: `/prestador/paciente/[id]` + `GET /api/prestador/patients/[id]/overview`
- Link “Histórico completo” na tela de atendimento

### Modos demo e operação (PR #71)

- Dual SQLite: `demo.db` (massa seed) + `operation.db` (bootstrap mínimo, dados reais)
- Seletor em **`/interno/seguranca`** → card **Base de dados — demo ou operação** (ADMIN)
- Persistência operação via Netlify Blobs (`sqlite-blob-persistence.ts`)
- Variáveis: `DUAL_DATA_STORE`, `DATA_STORE_MODE`, `APP_MODE`, `RUN_SEED_ON_BUILD`
- Docs: [`OPERACAO_DADOS.md`](OPERACAO_DADOS.md)

### Processo e qualidade

- Fluxo **dev-first**: PRs integram em `dev` antes de `main` (PR #70)
- Build CI sem `DATABASE_URL` no prerender dos portais
- Teste PPU com slot determinístico
- Refator `getPrisma()` centralizado em serviços

---

## Incrementos v1.0.2 (PR #77)

Separação **Sistema Bibi** (produto vendido) × **clínica** (tenant cliente):

| Camada | Identidade | Onde |
|--------|------------|------|
| Plataforma | **Sistema Bibi** | Landing `/`, marketing |
| Login público | **Portal da clínica** + *Powered by Sistema Bibi* | `/login`, `/interno/login`, etc. |
| Portais logados | Marca do tenant (ex.: **Clínica Horizonte**, **VitaCare**) | Após autenticação |

### Implementação

- `PLATFORM_BRANDING`, `LOGIN_PORTAL_BRANDING`, `CLINIC_BRANDING_DEFAULTS` em `src/lib/theme/tokens.ts`
- `getPlatformBranding()` — fixo, sem ler tenant do banco
- `getLoginBrandingFromHeaders()` — tenant por domínio customizado ou shell neutro
- Tenant demo renomeado: **Clínica Horizonte** (substitui “Clínica Bibi Saúde” no seed)

Ver também: [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md) § White label.

---

## Cadastros (estado na linha 1.0)

CRUD básico em `/interno/cadastros`:

- Beneficiário (nome, CPF, nascimento, telefone, empresa)
- Empresa (nome, CNPJ, status CRM)
- Procedimento (código, nome, categoria, preço)
- Usuário (nome, e-mail, senha, role, vínculos)

Melhorias de mercado (campos ANS/B2B, validação CPF/CNPJ, RBAC APIs) → **v1.2.0** em produção. Ver [`V1_2.md`](V1_2.md).

---

## Validação

```bash
npm run lint
npm run test
npm run test:e2e
npm run pre-release
```

---

## Referências

- [`RELEASES.md`](RELEASES.md) — pacotes fechados
- [`OPERACAO_DADOS.md`](OPERACAO_DADOS.md) — demo vs operação
- [`V1_1.md`](V1_1.md) — Care Chart (incorporado em 1.2.0)
- [`V1_2.md`](V1_2.md) — pacote atual em produção
- [`HISTORICO_2026-06-21.md`](HISTORICO_2026-06-21.md)
