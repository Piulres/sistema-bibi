# Sistema Bibi - ServiceOS v2.3 — Escopo e changelog

Linha **v2.3.x** — pacote base **v2.3.0** + patch **v2.3.1** (versão no title/footer).

| Meta | Valor |
|------|-------|
| **Versão deste pacote** | `2.3.1` (histórico) |
| **Tag git** | `v2.3.1` |
| **Pacote base** | `2.3.0` @ `374b13e` / tag `v2.3.0` |
| **Branch** | `dev` / `main` |
| **Produção atual** | Ver [`RELEASES.md`](RELEASES.md) — **v3.0.1** |
| **Data desta revisão** | 25/07/2026 |

---

## 1. Resumo executivo

A v2.3 consolida **assistente operacional serverless e multi-nicho**, **fluxo VET completo**, **módulo Engenharia Civil (CONSTRUCTION)**, **onboarding guiado fase 2** e **documentação Swagger da API**.

---

## 2. Changelog consolidado

### Assistente — correções serverless ([#148](https://github.com/Piulres/sistema-bibi/pull/148))

- `sessionState` e `pendingActionId` assinados (HMAC) — sem `Map` em memória
- Confirmação de ações persiste entre requests na Netlify
- **JTI one-time** na confirmação — replay bloqueado (Blobs + fallback dev)
- Anti-repetição em follow-ups e extração multi-turno
- Analytics: eventos `ASSISTANT_TOOL_*` na timeline (`/interno/auditoria`)
- Doc: [`ASSISTENTE_SERVERLESS.md`](../produto/ASSISTENTE_SERVERLESS.md)
- Testes stateless em `tests/integration/assistant-flow.test.ts`

### Assistente — multi-nicho e VET ([#149](https://github.com/Piulres/sistema-bibi/pull/149))

- RAG por nicho (`niche-knowledge.ts`) nos 6 segmentos
- Procedimentos do catálogo do tenant (`procedure-match.ts`)
- Copiloto contextual por rota (`page-suggestions.ts`)
- VET: `search_pets`, agendamento com `petId`, auto-seleção de pet único
- Testes: `tests/unit/assistant-niche.test.ts`

### Onboarding v3 ([#152](https://github.com/Piulres/sistema-bibi/pull/152))

Detalhe: [`produto/ONBOARDING_TOUR.md`](../produto/ONBOARDING_TOUR.md)

| Antes (v2.2) | Agora (v2.3) |
|--------------|--------------|
| Tour linear (17 passos no interno) | Tour principal ~5 passos + micro-tours por rota |
| Passos de página raramente vistos | Micro-tour na **1ª visita** a cada módulo |
| Pular não persistia | `dismissed` no `localStorage` |
| Nav invisível no mobile | `data-tour-nav` no drawer + botão Tour visível |
| `ONBOARDING_VERSION = 2` | `ONBOARDING_VERSION = 3` |

**Hotspots:** Cliente 360°, PIX, PEP, demo reset, dual-store, mapa CRUD.

**Testes:** `tests/unit/onboarding.test.ts` (21 casos)

### Engenharia Civil — CONSTRUCTION

| Área | Rotas |
|------|-------|
| Interno | `/interno/projetos`, pipeline, financeiro, `[id]` |
| Prestador | `/prestador/campo` — RDO |
| PJ / Beneficiário | `/pj/projetos`, `/beneficiario/obras` |

**Demo:** `/?tenant=build` · `operacao@build.demo` / `bibi123`

**Docs:** [`segmentos/construction/README.md`](../segmentos/construction/README.md)

**Testes:** `tests/unit/project.test.ts`, `tests/api/construction-projects.test.ts`

### API e massa demo

- Swagger UI em `/api/docs` — 123 paths OpenAPI
- Seeds ricos multi-segmento + `SEED_PROFILE` (`market` \| `operation-1y`)
- Doc: [`plataforma/API_DOCS.md`](../plataforma/API_DOCS.md) · [`plataforma/MASSA_TESTES.md`](../plataforma/MASSA_TESTES.md)

---

## 3. Validação em dev

```bash
git checkout dev && git pull origin dev
npm run db:push && npm run db:seed   # VM nova
npm run dev
```

| Fluxo | Credencial | URL |
|-------|------------|-----|
| Tour principal | `faturamento@bibi.health` | `/interno/dashboard` |
| Micro-tour faturamento | mesmo | `/interno` |
| Micro-tour agenda | `recepcao@bibi.health` | `/interno/agenda` |
| Obras Build | `operacao@build.demo` | `/interno/projetos` |
| OpenAPI | — | `/api/docs` |

Limpar tour: `localStorage.removeItem('bibi_onboarding')`

**Evidências:** [`versoes/V2_3.md`](V2_3.md)

### Testes automatizados

| Etapa | Resultado |
|-------|-----------|
| `npm run lint` | ✅ |
| `npm run docs:verify` | ✅ |
| `npm run openapi:verify` | ✅ |
| `npm run db:verify` | ✅ |
| `npm test` | ✅ 495+ Vitest |
| `npm run test:e2e` | ✅ 138 E2E |
| `npm run pre-release` | ✅ |

---

## 4. Publicação

✅ **v2.3.0** — deploy `6a6436ef` @ `374b13e` (`bibi-poc-2026-07-25a`).  
✅ **v2.3.1** — title/footer com versão + `PLATFORM.release` (`bibi-poc-2026-07-25b`).  
Ver [`RELEASES.md`](RELEASES.md).

### Patch v2.3.1

- `PLATFORM.release` (semver) sincronizado com `package.json`
- Title da home/layout: `… v2.3.1 — …`
- Footer da home: `ServiceOS v2.3.1` + copyright com versão
