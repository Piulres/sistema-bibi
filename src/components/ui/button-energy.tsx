/**
 * Botão com estilos Energia Brasileira — reexporta o componente canônico `Button`.
 * Use variantes `primary` (Dark Slate) e `accent` (Orange) para CTAs da identidade.
 */
export { default as Button, default } from "@/components/ui/Button";
